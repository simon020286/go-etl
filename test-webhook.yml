triggers:
  - name: webhook1
    type: webhook
    config:
      method: GET
      path: test

steps:
  - name: output
    type: stdout
    inputs:
      - webhook1
    config:
      value: "Webhook received data: ctx.webhook1.default"