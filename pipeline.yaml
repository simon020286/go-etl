steps:
  - name: read_file
    type: file
    config:
      path: input.txt

  - name: transform1
    type: uppercase
    inputs: 
      - read_file
    config:
      value: '{{ .read_file }}'

  - name: transform2
    type: map
    inputs: 
      - transform1
    config:
      fields:
        - name: first_char
          value: '{{ .transform1 }}'
          type: string
        - name: length
          value: '{{ len .transform1 }}'
          type: int

  - name: print
    type: stdout
    inputs: 
      - transform1
    config:
      value: '{{ .transform1 }}'