steps:
  # Cron trigger that fires every 10 seconds
  - name: every_10s
    type: cron
    config:
      schedule: "@every 10s"

  # Step that prints when the cron triggers
  - name: print_time
    type: stdout
    inputs: ["every_10s"]
    config:
      value: "ctx.every_10s.timestamp"
