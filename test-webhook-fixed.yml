steps:
  - name: webhook1
    type: webhook
    config:
      method: GET
      path: test
  - name: output
    type: stdout
    inputs:
      - webhook1
    config:
      value: "`Webhook received data: ${ctx.webhook1.valore}`"